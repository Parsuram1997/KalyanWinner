rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is not secure for production.
    // Allows any user to read or write to any document.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
